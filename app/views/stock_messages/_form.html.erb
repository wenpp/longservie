<%= form_for(@stock_message) do |f| %>
  <% if @stock_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stock_message.errors.count, "error") %> prohibited this stock_message from being saved:</h2>

      <ul>
      <% @stock_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_field :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
